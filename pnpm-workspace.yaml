# pnpm blocks dependency build scripts by default. esbuild (bundler) and
# workerd (Cloudflare runtime) need their postinstall to fetch native binaries,
# so allow them explicitly — otherwise `vite build` / `wrangler` can't run.
allowBuilds:
  esbuild: true
  workerd: true
